# 成桂零对接服务站 — 使用说明

一个面向 **男装 / 童装 / 大码男装 / 家居服** 的款式对接与提报站点，参考「麦杞岐对接服务站」的结构制作，包含 7 大模块与 3 个实用工具页。

---

## 一、站点包含哪些文件

```
成桂零对接服务站/
├── index.html              # 首页（导航总览 + 7 大模块）
├── title-optimization.html # 标题优化工具（选词生成中英文标题）
├── image-optimization.html # AI 指令生成器（生成图片优化提示词）
├── market-demand.html      # 市场需求图片区（高优相似款提报）
├── plan-demand.html        # 企划需求展示
├── top-products.html       # 去年热销展示
├── images/                 # 图片资源（当前为占位图，可替换为实拍图）
├── elements/
│   ├── style.css           # 整站配色 / 样式（Apple 高级配色）
│   ├── common.js           # 公共交互脚本（一般无需改动）
│   ├── data-*.js           # ★ 所有可编辑的内容数据
│   ├── data-full-categories.js  # 全量类目（2837 行，供类目筛选页使用）
│   └── category-filter.js  # 类目筛选页逻辑
├── category.html           # 类目筛选工具（按一层/二层/季节/分组/性别筛选）
├── plan-detail.html        # 企划详情子页模板（?id=N 展示对应企划+PDF）
└── assets/pdf/
    ├── market-visual-demand.pdf  # 市场视觉需求文档（占位，待替换）
    └── plan-1.pdf ... plan-6.pdf # 各企划PDF（按需放入，见第六.2 节）
```

---

## 二、如何打开

用浏览器直接双击 `index.html` 即可，无需联网、无需部署。所有页面间通过相对路径互相跳转。

---

## 三、如何更新内容（最重要）

站点所有"文字内容"都由 `elements/` 下的 `data-*.js` 数据文件控制，**用任意文本编辑器（记事本 / VSCode 等）修改保存后，刷新页面即可生效**，无需改动 HTML。

| 想改的内容 | 编辑文件 | 说明 |
|-----------|---------|------|
| 类目指引（首页概览） | `data-categories.js` | 一级类目 items 里的 name / path / note / tags |
| 类目筛选（全量） | `data-full-categories.js` | 由类目 Excel 生成的 2837 行数据，更新表格后重新生成即可 |
| 国家站点热搜词 | `data-hotwords.js` | 每个区域的 `{t:词, hot:热度}`，hot≥70 高亮为热门 |
| 近期通知 | `data-notices.js` | `{date,title,tag,content}`，tag 建议：重要/更新/已上线/截止 |
| 市场需求（高优） | `data-market-demand.js` | `{no,spu,cat,style,img,tag}` |
| 企划需求 | `data-plan-demand.js` | 6 个企划分组 + 款式条目 `{no,title,img,pdf}` |
| 去年热销 | `data-top-products.js` | 按区域分组的热销款式 |
| 标题优化选词库 | `data-title-words.js` | 四类目各维度的 `{cn,en}` 词（含风格/图案/颜色/材质/领型/袖长/版型/细节/功能/季节/节日等） |
| AI 指令选项/模板 | `data-visual-prompts.js` | 类目、场景、风格、模板等 |

> 小技巧：改完数据后，把文件保存为 **UTF-8** 编码；如遇乱码请检查是否为 UTF-8。

---

## 四、如何替换图片（市场需求 / 企划 / 热销）

1. 把你的真实款式图片放入 `images/` 目录（png / jpg 均可）。
2. 在对应 `data-*.js` 里把某一条的 `img: "images/旧文件.svg"` 改成 `img: "images/你的图片.png"`。
3. 保存并刷新页面即可。

> 当前 `images/` 里是系统生成的占位图（SVG），仅用于演示排版，请替换为真实、有授权的图片素材。

---

## 五、如何替换市场视觉需求 PDF

把真实文档命名为 `market-visual-demand.pdf` 覆盖 `assets/pdf/` 下的占位文件即可；若改了文件名，请在 `elements/data-visual-prompts.js` 顶部把 `pdfUrl` 同步改掉。

---

## 五之二、类目筛选工具

首页导航「类目筛选」或类目指引模块的入口横幅都进入 `category.html`：

- **品类标签**：标码男装 / 大码男装 / 童装 / 家居服，显示各自条目数。
- **级联筛选**：按 一层品类 → 二层品类 → 季节 逐级筛选；童装支持按 分组（男童/女童/婴童）；家居服支持按 人群/性别。
- **关键词搜索**：输入品类名或路径关键词即时过滤。
- **每条结果**可一键「复制路径」，用于提报填类目。
- 数据来自 `elements/data-full-categories.js`（由类目 Excel 生成）。更新表格后，把新数据重生成并替换该文件即可。

## 五之二、企划需求子页面

每个企划卡片点「查看企划款式 进入子页面 →」或首页对应卡片，会进入 `plan-detail.html?id=N` 独立子页，展示：该企划分组文案 + 款式网格 + **企划材料 PDF 预览区**。

- 数据在 `data-plan-demand.js`，已预置 6 个企划（秋冬外套/童装校服/家居服/大码舒适/节日礼盒/爆款补款），后两个待补充款式。
- **上传各企划 PDF**：把 PDF 命名为 `plan-1.pdf`…`plan-6.pdf` 放入 `assets/pdf/`，与分组序号一一对应；放入后刷新对应子页面即自动预览。未放入时子页面显示"待上传"占位，不影响其它功能。

## 六、如何部署上线（可选）

本地文件即可直接使用。如需让同事通过链接访问，把整个文件夹上传到任意静态托管（GitHub Pages / 内网静态服务器 / OSS 等），保持目录结构不变即可。上传后浏览器打开首页链接即为线上站点。

---

## 七、标题优化工具使用流程（对应参考站）

1. 进入「标题优化工具」页面。
2. 选择品类（男装 / 童装 / 大码男装 / 家居服）。
3. 按维度依次选词（品类、风格、图案、颜色、材质、领型、袖长、版型、细节、功能、季节、节日活动等），可跳过；也可点「随机组合」。
4. 点「生成标题」，中英文标题会同步生成；点「复制」后粘贴使用。
5. ⚠️ 生成标题仅供参考，请检查后使用。

---

## 八、免责声明

站内展示的图片与款式均为参考模板，请勿直接复制或侵权使用；提报商品须具备合法来源与授权。